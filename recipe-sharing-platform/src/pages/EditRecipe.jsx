import React from 'react'
import CreateRecipe from './CreateRecipe'

export default function EditRecipe() {
  // Reuse CreateRecipe form for editing; in a real app you'd load data.
  return <CreateRecipe />
}
